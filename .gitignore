###############################################################################
# MATLAB / SIMULINK COMMON
###############################################################################

# MATLAB autosave and backup files
*.asv
*.m~
*.mlx.autosave
*.autosave
*.bak

# MATLAB preferences / local settings (machine-specific)
*.mlproj
*.prj
*.settings
*.user

# MEX-files (compiled binaries; platform-specific)
*.mex*
*_mex.*

# P-code (obfuscated compiled MATLAB code)
*.p

# Simulink generated folders (often appear even if project is image-based)
slprj/
sccprj/
codegen/
*.mdl.autosave
*.slx.autosave

###############################################################################
# DIGITAL IMAGE PROCESSING SPECIFIC (DATA, OUTPUTS, MODELS)
###############################################################################

# === IMAGE DATA / DATASET FOLDERS ============================================
# If your dataset is LARGE, you usually do NOT want it in Git.
# Instead: keep a README with download instructions, or use Git LFS.
#
# WARNING: only keep these ignores if you really store big image datasets here.
# If your datasets are small and you want them inside the repo, remove or adjust.

data/
dataset/
datasets/
images/
raw_images/
input_images/
training_data/
validation_data/
test_data/

# If you have specific subfolders for large datasets, list them explicitly:
# data_raw/
# data_external/

# Image file formats that often belong to LARGE datasets
# (Comment these out if you want to track some sample images!)
*.tif
*.tiff
*.bmp
*.jpg
*.jpeg
*.png
*.gif
*.ppm
*.pgm

# If you want to KEEP some example images in the repo,
# put them in a folder like `examples/` and do NOT ignore that folder:
!examples/
!examples/**
!demo_images/
!demo_images/**

# === RESULTS / OUTPUTS / INTERMEDIATE FILES ==================================
# These are typical folders where scripts save processed images or results.
# They should be reproducible from code and do not need to be versioned.

results/
output/
outputs/
tmp/
temp/
cache/
logs/
log/
experiments/
runs/
reports/
figures/
plots/
masks/
segmentations/
features/

# Common image/video outputs generated by the code
*.avi
*.mp4
*.mov

# Confusion matrices, prediction dumps, etc., if generated automatically
predictions/
metrics/
evaluation/
tensorboard/

# === MACHINE LEARNING / DEEP LEARNING CHECKPOINTS ============================
# If you use Deep Learning Toolbox or custom training loops in MATLAB,
# trained models and checkpoints can become VERY large.

models/
trained_models/
checkpoints/
weights/
snapshots/

# MAT-files that are likely large and generated automatically.
# NOTE: This is aggressive. If you have small, curated .mat files that are
# part of your source (e.g., small example datasets), put them in a folder
# like `data_small/` or `examples/` and explicitly allow that folder.

*.mat

# But keep small demo MAT files if they are in these folders:
!examples/**/*.mat
!demo_data/**/*.mat
!data_small/**/*.mat

###############################################################################
# EDITOR / IDE / OS NOISE
###############################################################################

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Linux temp
*~

# VS Code
.vscode/
*.code-workspace

# IntelliJ / CLion / other JetBrains IDEs
.idea/
*.iml

# MATLAB code analyzer cache (if any custom path)
.mlintcache

###############################################################################
# DOCUMENTATION / BUILD ARTIFACTS
###############################################################################

# Generated documentation (publish, live script exports, etc.)
html/
doc/
docs_build/
helpsearch/

# LaTeX / report build artifacts
*.aux
*.log
*.out
*.toc
*.lof
*.lot
*.synctex.gz

###############################################################################
# MISC / CUSTOM PROJECT-SPECIFIC
###############################################################################

# Python virtual envs if you mix Python + MATLAB
venv/
.env/
.envrc/
__pycache__/
*.pyc

# Jupyter notebooks checkpoints (if you mix Jupyter + MATLAB kernel or Python)
.ipynb_checkpoints/

# Any other local-only script configs
*.local.*
